{
  "name": "vampcrawlers-guide",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.7.2",
    "@astrojs/starlight": "^0.34.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
